{
  "name": "canvas-sandbox-ball",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "browser-sync start --server --files '*.html, css/*.css, js/*.js' --no-notify --no-ghost-mode --no-open & npm run watch",
    "watch": "babel src -d js -w",
    "build": "babel src -d js"
  },
  "author": "tsuyoshiwada",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-preset-es2015": "^6.6.0",
    "browser-sync": "^2.11.1"
  }
}
